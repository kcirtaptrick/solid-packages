{
  "name": "solid-packages",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "./scripts/configure-references",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel",
    "signals": "yarn workspace solid-signals"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.6.2",
    "tsd": "^0.20.0",
    "tsm": "^2.2.1",
    "typescript": "^4.6.4",
    "uvu": "^0.5.3",
    "watchlist": "^0.3.1"
  }
}
