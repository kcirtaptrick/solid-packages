{
  "name": "solid-packages",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "./scripts/configure-references",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel",
    "signals": "yarn workspace solid-signals",
    "utils": "yarn workspace solid-u"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.8.0",
    "tsd": "^0.20.0",
    "tsm": "^2.2.1",
    "typescript": "^4.9.3",
    "uvu": "^0.5.3",
    "uvu-watch": "^1.0.11"
  }
}
